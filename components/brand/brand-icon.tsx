import type { LucideIcon } from 'lucide-react-native';
import { PawPrint, Tractor } from 'lucide-react-native';

/** Default icon for farms, herds, and animal counts (replaces bird/poultry imagery). */
export const FarmIcon: LucideIcon = PawPrint;

/** Hero / onboarding / auth header mark. */
export const BrandMarkIcon: LucideIcon = Tractor;
